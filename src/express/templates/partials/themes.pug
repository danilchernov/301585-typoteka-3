ul.themes
  each category in categories
    if (category.count != 0)
      li.themes__item
        a.themes__item-link(href=`/articles/categories/${category.id}`)
          | #{category.name}
          sup #{category.count}

