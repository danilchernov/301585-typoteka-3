extend /layout

block content
  main
    h1.visually-hidden Регистрация пользователя
    section
      .popup.popup--registration.popup--anti
        a.popup__button.button.button--popup-close(aria-label='Закрыть окно')
          | Закрыть
          | окно
        h2.popup__title Войти
        .popup__form.form.form--log-in
          form(action='/login' method='POST' enctype="multipart/form-data" autocomplete="off")
            .form__field
              label
                input(type='email' name='email' value=`${email ? email: ''}` placeholder='Электронная почта' required)
            .form__field
              label
                input(type='password' name='password' value=`${password ? password: ''}` placeholder='Пароль' required)
            if validationMessages
              p.form__error-message При регистрации произошли ошибки:
                ul.form__errors
                each error in validationMessages
                  li.form__error #{error}
            button.form__submit-btn.form__submit-btn--log-in.button.button--colored(type='submit') Войти
